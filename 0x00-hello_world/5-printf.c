#include <stdio.h>
/**
 * main - mandatory in all programs
 * Return: return
 */
int main(void)
{
printf("with proper grammar, but the outcome is a piece of art,", stdout);
	getchar();
	return (0);
}
